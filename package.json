{
  "name": "ai-agent-platform",
  "version": "1.0.0",
  "description": "AI Agent Creation Platform for Websites",
  "private": true,
  "workspaces": [
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"cd backend && python -m uvicorn main:app --reload\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "cd backend && python -m uvicorn main:app --reload",
    "build": "npm run build --workspace=frontend",
    "test": "npm run test --workspaces && cd backend && pytest",
    "test:ci": "npm run test:ci --workspaces && cd backend && pytest -q",
    "lint": "npm run lint --workspaces && cd backend && ruff check .",
    "clean": "rm -rf node_modules && npm run clean --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0",
    "python": ">=3.11.0"
  }
}
